[package]
name = "example"
version = "0.0.4"
edition = "2024"

[dependencies]
afast = { path = "../afast" }
tokio = { version = "1", features = ["full"] }

[features]
default = ["ws", "http", "tcp", "js"]
js = ["afast/js"]
tcp = ["afast/tcp"]
http = ["afast/http"]
ws = ["afast/ws"]
